extends ../../_component/layout/template

block cool-config
    -
        var myTitle = 'Learn SQL Syntax';
        var myDescription = 'SQL is a database';
        var landingPage = 'SQL';
        var activeLink = '/sql/syntax';
        
    
block awesome-content
    h1=myTitle
    p=myDescription
    h2 SQL Syntax
    p A database most often contains one or more tables. Each table is identified by a name (e.g. "Customers" or "Orders"). Tables contain records (rows) with data.
    p In this tutorial we will use the well-known Northwind sample database (included in MS Access and MS SQL Server).
    